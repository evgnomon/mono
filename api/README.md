Contracts between components: protobuf definitions, OpenAPI specs, JSON schemas.
Build steps compile these and install outputs to $(PREFIX)/include/ or $(PREFIX)/share/.
Consumers depend on the build tree, not on this source directory.
