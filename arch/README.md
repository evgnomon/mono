Platform-specific implementations of the same interface, selected at build time.
Examples: arch/linux/, arch/darwin/, arch/windows/ for OS-level differences,
or arch/aws/, arch/azure/ for cloud provider backends.
